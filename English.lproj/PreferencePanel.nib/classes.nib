{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {cancel = id; ok = id; restore = id; }; 
            CLASS = PreferencePanel; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                antiAlias = id; 
                blinkingCursor = id; 
                copySelection = id; 
                hideTab = id; 
                macnavkeys = id; 
                openAddressBook = id; 
                optionKey = id; 
                prefPanel = id; 
                promptOnClose = id; 
                scrollbackLines = id; 
                silenceBell = id; 
                tabViewType = id; 
            }; 
            SUPERCLASS = NSResponder; 
        }
    ); 
    IBVersion = 1; 
}